---
description: Read the project context docs and report the current state before starting work
---

Get fully oriented on this project, then give me a brief state-of-play. Steps:

1. Read, in order: `docs/PORTFOLIO_THESIS.md`, `docs/CHASSIS_SPEC.md`, `spec/tracker-spec.md`, and `BUILD_LOG.md` (the `NEEDS INPUT` section at the top and the newest few entries at the bottom).
2. Skim recent history: `git log --oneline -8` and `git status`.
3. Report back in ~10 lines: what this project is, where it stands (is it live? is the working tree clean? is CI green?), any open `NEEDS INPUT` items, and the single most logical next task.

Do not change anything yet — end by asking what I want to work on.
